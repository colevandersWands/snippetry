#as palimpsest
#in dialogue

let free(dom|) be united
let (crackle|pop) be division

to discriminate—others?—:
	if—

let reduceTo be input "a number"

let x be discriminate—reduceTo—
if is a "number"

-> is is am is are 
	we are united

united, individuals

