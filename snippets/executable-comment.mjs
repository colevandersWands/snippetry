import { _ } from './executable-comment._.mjs';
import { _spoken_ } from './executable-comment._spoken_.mjs';
import { _written_ } from './executable-comment._written_.mjs';

export { _, _spoken_, _written_ };

export default _;

// tags: minibrary

// --- wait, wat? ---

const { __ } = _;
// const { __ } = _spoken_;
// const { __ } = _written_;

__.This.curiosity.lets.you.write.any.chain.you.want(...__.within.JS.syntax);
__.Your.comments.will.be.executed, __.but.they["won't"].DO.anything;
__.Operators.are.now(_.sort.of).punctuation - __.with.a.little.imagination;
__.What.are.you.waiting.for?.___.Execute.your.comments['!'];
