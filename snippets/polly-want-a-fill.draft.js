(function pollyWantAFill(target) {
  // recursively add a proxy to every object 
	// when a missing property is accessed and called
	// call this function instead
	() => alert(`${missing} want a pollyfill`)
})(window)

# tags: pollyfill