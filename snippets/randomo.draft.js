const decrypted = `some code written like normal english (or python?)`;

const key = {
  ['normal term']: 'js syntax',
};

const encrypted = '"encrypt" the code to real JS';

eval(encrypted);

// reference: https://www.vidarholen.net/~vidar/An_Empirical_Investigation_into_Programming_Language_Syntax.pdf
